#include "main.h"

/**
 * leet - a function that encodes a string using rot13
 * @str: string
 *
 * Return: encodded string
*/
char *leet(char *str)
{
    arr1[] = {a, A, e, E, o, O, t, T, l, L};
    arr2[] = {4,3,0,7,1}

    for (i )
}