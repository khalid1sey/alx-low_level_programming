#include <stdio.h>

/**
 * main - entry point
 *
 * @argc: arg counter
 * @argv: argument vector
 *
 * Return: ALways 0 Success
 */

int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
