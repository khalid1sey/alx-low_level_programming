#include "main.h"
#include <stdlib.h>

/**
 * print_alphabet_x10 - a function that checks lowercase character.
 *
 *@c: given character
 * Return: 1 (Success) | 0 
*/
int _islower(int c)
{
    

    int i;

    for (i = 65; i <= 122; i++)
    {
        if (atoi(c) <= 97 && atoi(c) <= 122)
        {
            return (1);
        } else{
            return (0);

    return (0);
}
